service: my-service

plugins:
  - "../../index"

custom:
  bundle:
    aliases:
      - "is-sorted": false

provider:
  name: aws
  runtime: nodejs16.x

functions:
  hello:
    handler: handler.hello
